import BScroll from '@better-scroll/core';
import { EaseItem } from '@better-scroll/shared-utils';
import { Page, Position } from './SlidePage';
export declare type SlideOptions = Partial<Config> | boolean | undefined;
export interface Config {
    loop: boolean;
    el: HTMLElement | string;
    threshold: number;
    stepX: number;
    stepY: number;
    speed: number;
    easing: {
        style: string;
        fn: (t: number) => number;
    };
    listenFlick: boolean;
    disableSetWidth: boolean;
    disableSetHeight: boolean;
}
declare module '@better-scroll/core' {
    interface Options {
        slide?: SlideOptions;
    }
}
export default class Slide {
    scroll: BScroll;
    static pluginName: string;
    private page;
    private slideOpt;
    private thresholdX;
    private thresholdY;
    private hooksFn;
    private resetLooping;
    private isTouching;
    private willChangeToPage;
    private resizeTimeout;
    constructor(scroll: BScroll);
    init(): void;
    resize(): boolean;
    next(time?: number, easing?: EaseItem): void;
    prev(time?: number, easing?: EaseItem): void;
    goToPage(x: number, y: number, time?: number, easing?: EaseItem): void;
    getCurrentPage(): Page;
    nearestPage(x: number, y: number): Page & Position;
    destroy(): void;
    private initSlideState;
    private initThreshold;
    private cloneSlideEleForLoop;
    private amendCurrentPage;
    private shouldSetWidthHeight;
    private clearSlideWidth;
    private setSlideWidth;
    private clearSlideHeight;
    private setSlideHeight;
    private goTo;
    private flickHandler;
    private getAnimateTime;
    private modifyScrollMetaHandler;
    private scrollMoving;
    private pageWillChangeTo;
    private setTouchFlag;
    private registorHooks;
}
